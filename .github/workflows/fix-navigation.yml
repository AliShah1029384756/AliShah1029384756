# DISABLED — one-shot navigation polish workflow
# This file is kept only so GitHub stops re-creating notifications.
# The polish was already applied to script.js.
#
# To permanently remove: delete this file from the repo.

name: Apply Navigation Polish (disabled)

on:
  workflow_dispatch:  # manual only — never auto-runs

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is intentionally disabled."
